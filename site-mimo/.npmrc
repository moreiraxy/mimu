# Mesmo motivo do .npmrc da raiz: o Railway compila com NODE_ENV=production e
# o npm pula devDependencies nesse modo. Vite, Tailwind e TypeScript vivem lá,
# e sem eles o build da landing page nem começa.
include=dev
