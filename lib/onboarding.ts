// A implementação canônica mora em @/lib/supabase — mantido aqui como
// re-export para não quebrar os imports já existentes (layout do onboarding,
// dashboard, bem-vindo etc).
export { getEmpresaAtual } from "@/lib/supabase";
