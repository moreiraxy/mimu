# Fora do contexto de build do Railway.
#
# site-v2/ e site-mimo/ são projetos SEPARADOS que vivem neste repositório
# como material da landing page — o Next daqui não os constrói nem os serve.
# Mandá-los para o builder é enviar ~36MB por deploy sem nenhum uso.
#
# site-mimo/ NÃO entra mais aqui: a landing page passou a ser construída em
# todo deploy (npm run build:lp) e servida pelo public/ do Next. Deixá-la de
# fora faria o deploy publicar a LP antiga sem avisar ninguém.
site-v2/

# Material de referência da marca, sem efeito em runtime.
brand/
